# Examples

This directory contains simple examples on how `llm-msg-io` can be used.

The examples also serve as an interactive integration tests for the library.

## Credentials

The examples use the same environment variables as the integration tests. See [integration-test/README.md](../integration-test/README.md) for more information.